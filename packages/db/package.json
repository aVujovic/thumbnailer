{
  "name": "@thumbnailer/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "setup": "prisma generate && prisma migrate deploy",
    "type-check": "tsc --noEmit",
    "test": "echo 'no tests in @thumbnailer/db' && exit 0"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "5.22.0",
    "typescript": "^5.6.0"
  }
}
